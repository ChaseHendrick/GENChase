# Minimal-winding generalizations: work in progress (2026-09-24)

A snapshot, saved so that paused agent work is not lost. **Nothing here is reviewed, and nothing here is a claim of the paper.** A result counts as established only where its own independent check in `results/` says `holds: true`; the prior-art checks were run from abstracts and search summaries where publisher sites were blocked.

- `results/wf_a2783863-dac.json`: five explorations (N vortices, the sphere, alpha-models, geometry, consequences of the bound) and the independent checks and prior-art reviews completed before the pause (18 agents).
- `results/wf_cfc28301-43f.json`: the ring families of Demina and Kudryashov (two rings plus a central vortex; three polygons), with the checks completed before the pause (4 agents). The three-polygon exploration was interrupted; its partial scripts are under `scripts/rings/three-polygons/`.
- `scripts/`: the agents' own programs and small outputs.
- `workflows/`: the orchestration scripts, to re-run the unfinished checks and the syntheses.
